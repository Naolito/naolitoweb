# Local dev admin token (optional). Use the same value in Cloudflare Pages env as ADMIN_API_TOKEN
VITE_ADMIN_API_TOKEN=

# Optional: Cloudflare Images delivery base (only needed if upload response doesn't include variants)
VITE_CLOUDFLARE_IMAGES_DELIVERY_URL=
VITE_CLOUDFLARE_IMAGES_VARIANT=public
